import 'package:flutter/material.dart';
import 'package:familyappver1/Screens/AlertPage/parts/body.dart';

class LandingBodyAlert extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: AlertBody(),
    );
  }
}